<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.quanli">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".them2"></activity>
        <activity android:name=".demo" />
        <activity android:name=".ThongKeKhac" />
        <activity android:name=".xemChiTiet" />
        <activity android:name=".theothang" />
        <activity android:name=".theongay" />
        <activity android:name=".them" />
        <activity android:name=".MainActivity" />
        <activity
            android:name=".trangchu2"
            android:label="@string/title_activity_trangchu" />
        <activity android:name=".taomapin" />
        <activity android:name=".load">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>